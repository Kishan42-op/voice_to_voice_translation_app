# Phase 4 Summary

## Work completed
- Added Firebase Authentication with email/password login and signup.
- Built the auth flow with `SplashActivity`, `AuthActivity`, `LoginFragment`, `SignupFragment`, and `MainActivity`.
- Saved signed-up users to Firestore under `users/{uid}`.
- Organized the app using Java, XML, MVVM, LiveData, Repository pattern, and Material Design.
- Added the required Firebase, lifecycle, and navigation dependencies.

## Result
- The app now supports authenticated sessions before entering the main app flow.

